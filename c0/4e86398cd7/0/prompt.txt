Image 1 — Paytm Payment Alert:
A buyer tried to pay ₹10 to farmer "Singampalli Tagore" via UPI. Paytm is showing a risk warning saying the payment may fail and suggesting to pay via mobile number or QR scan instead.
Image 2 — PhonePe Transaction Success:
A payment of ₹1,400 went through successfully but it was paid to "CALYNEX PRIVATE LIMITED" — which is wrong. This means somewhere in your code a hardcoded or wrong UPI ID is being used instead of the farmer's actual UPI ID. . i am getting this error whie doing this payment using our website while buying in cart so how we can fix

---

The bottom note is just a PhonePe limit warning — "You can pay up to ₹2,000 with QR codes via gallery. For more, pay with mobile number or scan QR code." This is PhonePe's own policy, not a bug in your app.now this error

---

how do i attach the screen shot

---

this is ubunut bro

---

"You can pay up to ₹2,000 with QR codes via gallery. For more, pay with mobile number or scan QR code." This is PhonePe's own policy, not a bug in your app.
this is exact warning

---

but what we have to do now

---

its there personal upi id only

---

vinayveeru12@ybl

---

Fix UPI payment flow using Android intent links:

1. Replace all upi:// deep links with intent:// links:

GPay:
intent://upi/pay?pa={upi_id}&pn={farmer_name}&am={amount}&cu=INR&tn=YourFamilyFarmer Order#Intent;scheme=upi;package=com.google.android.apps.nbu.paisa.user;end

PhonePe:
intent://pay?pa={upi_id}&pn={farmer_name}&am={amount}&cu=INR&tn=YourFamilyFarmer Order#Intent;scheme=phonepe;package=com.phonepe.app;end

Paytm:
intent://pay?pa={upi_id}&pn={farmer_name}&am={amount}&cu=INR&tn=YourFamilyFarmer Order#Intent;scheme=paytm;package=net.one97.paytm;end

2. Payment screen layout:
   - Show farmer name and UPI ID clearly at top
   - Show amount: "Pay ₹{amount} / ₹{amount} చెల్లించండి"
   - Three big buttons: GPay | PhonePe | Paytm
   - Each button opens that specific app with amount pre-filled
   - Below buttons show: "UPI ID: {upi_id}" with copy button

3. After payment:
   - "I have paid / చెల్లించాను" green button
   - Optional: upload payment screenshot
   - Save to Supabase: payment_method='upi', payment_status='pending_confirmation'
   - Show: "Waiting for farmer confirmation / రైతు నిర్ధారణ కోసం వేచి ఉంది"

4. Make sure farmer UPI ID is always fetched fresh from Supabase farmers table - never hardcoded

5. Test with minimum ₹5 amount - amounts below ₹5 get blocked by UPI apps