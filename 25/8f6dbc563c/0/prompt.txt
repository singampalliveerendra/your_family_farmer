For a consumer, move the "Log a Complaint" link to the top right corner, near the profile.
Add a "Log a Complaint" option on each individual order, so it's easy for a consumer to raise a complaint for a specific order.
On the Farmer Profile page, change the label "Farm Pickup Address" to "Farm Address".
In the Cart, change the text "I will pick from the farm" to "I will pick up".
When a consumer selects Pickup as the option during an order, make the Pickup location a mandatory (required) field.
When a consumer selects the "Home Delivery" option while ordering, the Pickup location field should be disabled.
Fix this bug: I am unable to place an order when I select Home Delivery and fill in the address, pin code, and phone number. (A screenshot is attached for reference.)
Show product/item titles in the selected language only. Right now it shows both languages with a "/" in between — only the chosen language should appear. . explain what you undersaynd in this

---

For a consumer, move the "Log a Complaint" link to the top right corner, near the profile.
Add a "Log a Complaint" option on each individual order, so it's easy for a consumer to raise a complaint for a specific order.
On the Farmer Profile page, change the label "Farm Pickup Address" to "Farm Address".
In the Cart, change the text "I will pick from the farm" to "I will pick up".
When a consumer selects Pickup as the option during an order, make the Pickup location a mandatory (required) field.
When a consumer selects the "Home Delivery" option while ordering, the Pickup location field should be disabled.
Fix this bug: I am unable to place an order when I select Home Delivery and fill in the address, pin code, and phone number. (A screenshot is attached for reference.)
Show product/item titles in the selected language only. Right now it shows both languages with a "/" in between — only the chosen language should appear. . explain what you undersaynd in these 8 fixex and features that given by client