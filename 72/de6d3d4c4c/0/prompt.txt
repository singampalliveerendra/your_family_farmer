Simplify UPI payment flow completely:

1. Remove all dynamic parameters from UPI link:
   - Remove amount pre-fill
   - Remove order ID
   - Remove transaction note
   - Keep ONLY farmer UPI ID

2. New UPI link format (static, just UPI ID):
   upi://pay?pa={farmer_upi_id}
   Open with window.location.href

3. Payment screen shows:
   - Farmer name and UPI ID clearly
   - Amount to pay in big text: "Pay ₹{amount} / ₹{amount} చెల్లించండి"
   - Instruction: "Enter this amount manually in your UPI app"
   - Three buttons: GPay | PhonePe | Paytm
   - Each opens their UPI app with only farmer UPI ID pre-filled
   - Buyer enters amount themselves in the UPI app

4. After payment:
   - "I have paid / చెల్లించాను" button
   - Save order with payment_status='pending_confirmation'

5. Farmer confirms in dashboard manually.is this work