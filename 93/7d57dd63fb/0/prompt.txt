Build complete Orders tracking system:

1. When consumer taps "Send to [farmer] / వాట్సాప్" button:
   - Before opening WhatsApp, save order to Supabase orders table:
     - buyer_name, buyer_phone, farmer_id, produce listing details, quantity, total price, pickup location, status='pending', created_at
   - Then open WhatsApp as normal

2. Create orders table in Supabase:
   CREATE TABLE orders (
     id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
     farmer_id uuid REFERENCES farmers(id),
     produce_listing_id uuid REFERENCES produce_listings(id),
     produce_name text,
     quantity numeric,
     unit text,
     total_price numeric,
     buyer_name text,
     buyer_phone text,
     pickup_location text,
     status text DEFAULT 'pending', -- pending | approved | declined
     created_at timestamptz DEFAULT now()
   );

3. Add Orders section in farmer dashboard:
   - New tab "Orders / ఆర్డర్లు" next to Active listings
   - Filter buttons: Today / ఈరోజు | This week / ఈ వారం | This month / ఈ నెల
   - Each order card shows:
     - Buyer name, phone, produce, quantity, total price, pickup location, time
     - Two buttons: "Approve / అంగీకరించు" (green) and "Decline / తిరస్కరించు" (red)
   - Approved orders move to order history with timestamp
   - Declined orders also saved with reason option

4. Orders stats in farmer dashboard header:
   - Show correct count: pending orders, approved this week, total revenue approved

5. Order history page:
   - All past approved/declined orders with full details
   - Filter by Today, This week, This month
   - Show total revenue for each time period
   - All data fetched live from Supabase orders table. before doing tell me what you understand

---

add as per the farmers comfort and visibilty they can accsess eaily

---

Error: Failed to run sql query: ERROR: 42P07: relation "orders" already exists

---

send me whole query

---

CREATE TABLE orders (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    farmer_id uuid REFERENCES farmers(id),
    produce_listing_id uuid REFERENCES produce_listings(id),
    produce_name text,
    quantity numeric,
    unit text,
    total_price numeric,
    buyer_name text,
    buyer_phone text,
    pickup_location text,
    status text DEFAULT 'pending',
    created_at timestamptz DEFAULT now()
  );

  ALTER TABLE orders ENABLE ROW LEVEL SECURITY;

  CREATE POLICY "public_insert" ON orders FOR INSERT WITH CHECK (true);
  CREATE POLICY "public_select" ON orders FOR SELECT USING (true);
  CREATE POLICY "public_update" ON orders FOR UPDATE USING (true);should i remove this

---

without removing that add new and add this new query?

---

next

---

- Region page improvements — Better discovery for buyers
  - Reviews system — Buyers leave feedback after pickup . implment these two very accurately

---

how to send a image to you

---

Orders / ఆర్డర్లు
Pending orders need your response

View history →
Today
This week
This month
📭
No pending orders

New orders will appear herei ordered strwabery but not showing request approve or declaine . and if i approve it have to save in orders and in history not getting anything

---

tell me how the consumers give the review the farmer